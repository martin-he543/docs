name: Update Post Index

on:
  push:
    branches:
      - main
    paths:
      - "posts/**/*.md"
      - "scripts/generate-post-index.mjs"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  regenerate-index:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Regenerate posts/index.json
        run: node scripts/generate-post-index.mjs

      - name: Commit updated index
        run: |
          if git diff --quiet -- posts/index.json; then
            echo "No index updates required."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add posts/index.json
          git commit -m "chore: refresh post index"
          git push
