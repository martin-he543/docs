# Second placeholder post

```post-data
{
  "date": "2026-08-10",
  "tags": ["placeholder"],
  "summary": "A second placeholder, mostly here to prove that the blog index sorts posts by date."
}
```

This post only exists to prove that the listing page can hold more than one entry, and that posts are sorted newest-first by their `date` field in the metadata block, not by filename.

Delete it, rename it, or edit it — whatever's useful. Nothing here is meant to be kept.
