# Cursor Customisation

```post-data
{
  "date": "2026-08-16",
  "tags": ["json","settings"],
  "summary": "My custom keybindings, extensions, themes, and more for Cursor.."
}
```

Cursor is a decent IDE, but it can be so much better.

## Custom Keybindings

```json
// Place your key bindings in this file to override the defaults
[
    {
        "key": "ctrl+alt+cmd+c",
        "command": "claude-vscode.sidebar.open"
    },
    {
        "key": "alt+cmd+z",
        "command": "editor.action.toggleWordWrap"
    },
    {
        "key": "alt+z",
        "command": "-editor.action.toggleWordWrap"
    },
    {
        "key": "ctrl+cmd+alt+shift+c",
        "command": "runCommands",
        "args": {
          "commands": [
            { "command": "git.stageAll" },
            { "command": "git.commitStaged" },
            { "command": "git.sync" }
          ]
        }
     }
]
```
